import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#9F0086',
    },
    secondary: {
      main: '#9A9A9A',
    },
    error: {
      main: '#D94C4B',
    },
  },
});

export default theme;
